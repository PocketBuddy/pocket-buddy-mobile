export enum ButtonType {
  Primary = 'primary',
  Secondary = 'secondary',
}

export enum BrandMode {
  Contain = 'contain',
  Cover = 'cover',
  Stretch = 'stretch',
  Repeat = 'repeat',
  Center = 'center',
}
