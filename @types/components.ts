export enum ButtonType {
  Primary = 'primary',
  Secondary = 'secondary',
}

export enum BrandMode {
  Contain = 'contain',
  Cover = 'cover',
  Stretch = 'stretch',
  Repeat = 'repeat',
  Center = 'center',
}

export enum IconType {
  Primary = 'primary',
  Secondary = 'secondary',
  Inactive = 'inactive',
}

export enum SpinnerSize {
  Small = 'small',
  Large = 'large',
}
